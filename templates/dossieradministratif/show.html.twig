{% extends 'base.html.twig' %}

{% block title %}Dossieradministratif{% endblock %}

{% block body %}
    <h1>Dossieradministratif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dossieradministratif.id }}</td>
            </tr>
            <tr>
                <th>Nom_etablissement</th>
                <td>{{ dossieradministratif.nomEtablissement }}</td>
            </tr>
            <tr>
                <th>Numero_offre</th>
                <td>{{ dossieradministratif.numeroOffre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dossieradministratif_index') }}">back to list</a>

    <a href="{{ path('dossieradministratif_edit', {'id': dossieradministratif.id}) }}">edit</a>

    {{ include('dossieradministratif/_delete_form.html.twig') }}
{% endblock %}
