{% extends 'base.html.twig' %}

{% block title %}Offredappel{% endblock %}

{% block body %}
    <h1>Offredappel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ offredappel.id }}</td>
            </tr>
            <tr>
                <th>NumeroOffre</th>
                <td>{{ offredappel.numeroOffre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('offredappel_index') }}">back to list</a>

    <a href="{{ path('offredappel_edit', {'id': offredappel.id}) }}">edit</a>

    {{ include('offredappel/_delete_form.html.twig') }}
{% endblock %}
